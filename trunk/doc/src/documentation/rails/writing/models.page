---
title: Models
inMenu: true
---
h2. Spec::Rails - Specifying Models

Model Specs live in $RAILS_ROOT/spec/models/ and provide access to fixtures.

h2. Expectations

Model Specs support the following custom expectations.
See "Spec::Rails::Expectations":../../../rdoc-rails/index.html for more detail.

h3. Records

<ruby>
Model.should have(:no).records
Model.should have(1).record
Model.should have(3).records
</ruby>

This is a shortcut for <code>Model.find(:all).length.should == 3</code>.

h3. Errors

<ruby>
model.should have(:no).errors_on(:attribute)
model.should have(1).error_on(:attribute)
model.should have(3).errors_on(:attribute)
</ruby>

h2. Example Model Specs

<ruby file="../example_rails_app/spec/models/person_spec.rb"/>
