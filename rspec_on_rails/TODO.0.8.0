Here's what needs to happen vis a vis the rails plugin before the 0.8.0 release.

== response.should return_code???? (Aslak: better: respond_with(501))

== response.should have_rjs(:effect)
== with_text
    this is mostly useful for nested expectations on have_tag